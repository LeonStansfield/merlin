#pragma once

#include <math.h>
#include <vector>

#include "raylib.h"
#include "includes/MerlinClass.h"

int pget(int x, int y)
{
    //loop through all gameObjects
    // Check if gameObject is collidable and colliding with x, y
    // if it is return collisioin layer
    // if not return 0
    return 0;
}